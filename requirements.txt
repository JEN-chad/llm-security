# Core API & Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
requests>=2.31.0

# Database
sqlalchemy>=2.0.25
psycopg2-binary>=2.9.9

# AI / ML / Modal
# modal>=0.60.0
transformers>=4.37.0
torch>=2.2.0

# System Requirements (Install manually if needed)
# - PostgreSQL 15+ (server)
# - libpq-dev (Linux) / PostgreSQL drivers (Windows)
# - git (for some pip packages)
